{
  "name": "angular-vite",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "vitest -c ./packages/tools/src/vite.test.config.ts",
    "storybook": "storybook dev -p 8080 -c ./packages/storybook/src --no-open",
    "serve": "vite -c ./packages/tools/src/vite.config.ts"
  },
  "workspaces": [
    "./packages/*"
  ]
}
